document.querySelectorAll(".blr").forEach(el=>{let b=el.dataset.blur;if(b){let r=parseFloat(b)/100*1.5;el.style.setProperty("--blr-amount",r+"rem");}}); document.querySelectorAll('.crcle-txt').forEach(el=>{const t=el.getAttribute('data-text'),r=el.getAttribute('data-radius')||100,c=t.split(''),a=360/c.length;el.style.position='relative';el.style.width=`${r*2}px`;el.style.height=`${r*2}px`;el.innerHTML='';c.forEach((ch,i)=>{const s=document.createElement('span'),ang=a*i-90,x=r+r*Math.cos(ang*Math.PI/180),y=r+r*Math.sin(ang*Math.PI/180);s.textContent=ch;s.style.position='absolute';s.style.left=`${x}px`;s.style.top=`${y}px`;s.style.transform=`translate(-50%,-50%) rotate(${ang+90}deg)`;el.appendChild(s);});});
